# k8s/secret.yaml
# Note: for production, prefer 'data:' with base64-encoded values.
apiVersion: v1
kind: Secret
metadata:
  name: devops-app-secret
  namespace: devops-app
type: Opaque
stringData:
  DB_URL: 'postgresql://user:pass@db:5432/app'
